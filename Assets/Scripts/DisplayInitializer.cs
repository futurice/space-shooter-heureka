using UnityEngine;
using System.Collections;

public class DisplayInitializer : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Screen.SetResolution(1920, 2160, false);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
